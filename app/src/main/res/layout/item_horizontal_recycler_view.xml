<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="item"
            type="com.example.spotifyclone.models.local.HomePageItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_start_end_10"
        android:layout_marginEnd="@dimen/margin_start_end_10">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/cover_image"
            imageFromUrl="@{item.image}"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:scaleType="fitCenter"
            android:src="@drawable/cover_album"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text_artists"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_top_4"
            android:maxLines="2"
            android:singleLine="false"
            android:text="@{item.name}"
            android:textColor="@color/primary_light_grey"
            android:textSize="@dimen/text_size_11"
            app:layout_constraintEnd_toEndOf="@+id/cover_image"
            app:layout_constraintStart_toStartOf="@id/cover_image"
            app:layout_constraintTop_toBottomOf="@id/cover_image" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>